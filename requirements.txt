# ── Core ML ────────────────────────────────────────────────
# Linux/Render uses the lean CPU-only wheel (no CUDA payload);
# macOS Apple-Silicon uses the standard wheel.
tensorflow-cpu==2.15.0 ; sys_platform == "linux"
tensorflow==2.15.0 ; sys_platform == "darwin"
numpy==1.26.4
scikit-learn==1.4.2
pillow==10.3.0

# ── API / serving ──────────────────────────────────────────
fastapi==0.110.1
uvicorn[standard]==0.29.0
python-multipart==0.0.9
jinja2==3.1.3
aiofiles==23.2.1

# ── Data / viz (used by notebook + dashboard) ──────────────
pandas==2.2.2
matplotlib==3.8.4
seaborn==0.13.2

# ── Load testing ───────────────────────────────────────────
locust==2.25.0

# ── Utilities ──────────────────────────────────────────────
requests==2.33.0
pydantic==2.7.1
